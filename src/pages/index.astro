---
import BlogCard from '@/components/BlogCard.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { buttonVariants } from '@/components/ui/button'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getRecentPosts } from '@/lib/data-utils'

const blog = await getRecentPosts(SITE.featuredPostCount)
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Home" />
  <section class="rounded-lg border">
    <div class="flex flex-col space-y-1.5 p-6">
      <h3 class="text-3xl leading-none font-medium">t4mpr</h3>
      <p class="text-muted-foreground text-sm">
        Security Researcher &bull; <span class="font-medium">CTF Player</span>
      </p>
    </div>
    <div class="p-6 pt-0">
      <p class="text-muted-foreground mb-2 text-sm">
        Welcome! This is a collection of my <strong class="text-foreground">CTF writeups</strong>, 
        <strong class="text-foreground">walkthroughs</strong>, and <strong class="text-foreground">security research</strong> 
        covering digital forensics, cryptography, web security, incident response, and more.
      </p>
      <p class="text-muted-foreground text-sm">
        I document my problem-solving process to share knowledge with the cybersecurity community. 
        Whether you're starting your journey in security or looking for different perspectives on challenges, 
        I hope you find these writeups helpful. Check out the <Link
          href="/blog"
          class="text-foreground"
          underline>latest writeups</Link
        > or visit my <Link
          href="https://github.com/t4mpr/ctf-writeups"
          class="text-foreground"
          underline
          external>GitHub</Link
        > for more.
      </p>
    </div>
  </section>
  <section class="flex flex-col gap-y-4">
    <h2 class="text-2xl font-medium">Latest posts</h2>
    <ul class="flex flex-col gap-y-4">
      {
        blog.map((post) => (
          <li>
            <BlogCard entry={post} />
          </li>
        ))
      }
    </ul>
    <div class="flex justify-center">
      <Link
        href="/blog"
        class={buttonVariants({ variant: 'ghost' }) + ' group'}
      >
        See all posts <span
          class="ml-1.5 transition-transform group-hover:translate-x-1"
          >&rarr;</span
        >
      </Link>
    </div>
  </section>
</Layout>
